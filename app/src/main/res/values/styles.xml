<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">15sp</item>
        <item name="android:fontFamily">@font/font_google</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Style for Section, Author, and Date TextView -->
    <style name="SectionAuthorDateStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin</item>
        <item name="android:layout_marginRight">@dimen/layout_margin</item>
    </style>

    <!-- Style for Author TextView -->
    <style name="AuthorTextViewStyle" parent="@style/SectionAuthorDateStyle">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:fontFamily">@font/font_google</item>
    </style>

    <!-- Style for Adresse TextView -->
    <style name="AdresseTextViewStyle" parent="@style/SectionAuthorDateStyle">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:fontFamily">@font/font_google</item>
    </style>

    <!-- Style for the Bottom Navigation View -->
    <style name="Widget.BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/font_google</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- The base Android Button facebook -->
    <style name="AppTheme.RoundedCornerMaterialButtonFacebook" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/rounded_shape_facebook</item>
        <item name="android:fontFamily">@font/font_google</item>
    </style>

    <!-- The base Android Button Email -->
    <style name="AppTheme.RoundedCornerMaterialButtonGoogle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/rounded_shape_google</item>
        <item name="android:fontFamily">@font/font_google</item>
    </style>

    <!-- Style for trailText TextView -->
    <style name="TrailTextViewStyle">
        <item name="android:layout_marginBottom">@dimen/layout_margin_8</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin</item>
        <item name="android:layout_marginRight">@dimen/layout_margin</item>
        <item name="android:ellipsize">end</item>
        <item name="android:fontFamily">@font/font_google</item>
        <item name="android:textSize">17sp</item>
        <item name="android:maxLines">6</item>
    </style>

</resources>
